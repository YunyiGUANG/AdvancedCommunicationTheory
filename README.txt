Each task is implemented in the corrresponding "*.m" files.
The experiment results are shown in the pdf file.
All the used functions are in the file "Wrappers".
All the used data are in the file "Data".
To run the task files, first need to add files "Wrapper" and "Data" into MATLAB path.